[pytest]
testpaths = tests
norecursedirs = v0-archive
addopts = --cov=skfd --cov-report=term-missing:skip-covered --cov-fail-under=85 --cov-config=.coveragerc
markers =
    adversarial: adversarial or negative tests for linker invariants
