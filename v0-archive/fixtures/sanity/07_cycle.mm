$( M0.2 Step 07 — dependency cycle between two units (should be rejected by linker once gate exists). $)

$c wff ( ) -> $.
$v ph ps $.

wph $f wff ph $.
wps $f wff ps $.

${
  $[ 07_unit_a_cycle.mm $]
  $[ 07_unit_b_cycle.mm $]
$}
