[pytest]
testpaths = tests
addopts = --cov=skfd --cov-report=term-missing:skip-covered --cov-fail-under=88 --cov-config=.coveragerc
markers =
    adversarial: adversarial or negative tests for linker invariants
