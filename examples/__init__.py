"""Repository-level examples.

Examples live outside the package tree on purpose:
- they must be runnable in editable installs without import cycles,
- they showcase how to construct IR and call the linker/verifier.
"""
