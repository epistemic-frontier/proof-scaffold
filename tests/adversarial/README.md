# Adversarial tests

This directory is reserved for Link Model v4 **ADV** regression tests.

In M0.2 we only provide placeholders to lock down:

- directory structure,
- naming conventions,
- stage expectations and diagnostic codes.

As `linker_v1` grows beyond bootstrap, each ADV should become an executable test
without changing file locations.

